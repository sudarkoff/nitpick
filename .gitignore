# Binaries
/nitpick
/bin/
*.test
*.out

# Local findings DB (if created inside the repo for dev)
/.nitpick-db/
/tmp/

# Editor
.DS_Store

# Local bg-session isolation toggle (not shared)
/.claude/settings.json
